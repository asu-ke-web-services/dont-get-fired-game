
// import React from 'react';
// import {expect} from 'chai';
// import {shallow} from 'enzyme';
// const {describe /* , it */
// } = global;

// import { default as Main } from '../main-page.jsx';

// describe('Main Page', () => {
  // const main = {
  //   name: 'RedFox inc',
  //   introduction: 'Hi im the CEO of RedFox inc',
  //   currentCompanyState: 'All good',
  //   endGameGoal: 'Win with in 5min'
  // };

  // it( 'has a name', () => {
  //   const wrapper = shallow(<Main main={main} />);
  //   expect(wrapper.contains('RedFox inc')).to.equal(true);
  // });
  // it( 'has introduction', () => {
  //   const wrapper = shallow(<Main main={main} />);
  //   expect(wrapper.contains('Hi im the CEO of RedFox inc')).to.equal(true);
  // });
  // it( 'has current company state', () => {
  //   const wrapper = shallow(<Main main={main} />);
  //   expect(wrapper.contains('All good')).to.equal(true);
  // });
  // it( 'has end game goal', () => {
  //   const wrapper = shallow(<Main main={main} />);
  //   expect(wrapper.contains('Win with in 5min')).to.equal(true);
  // });
// });
