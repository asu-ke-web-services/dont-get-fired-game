import React from 'react';

export default React.createClass({
    render () {
        return {
            <div>
                <div className="main-action-points">
                    {this.props.header}
                    {this.props.actionPoints}
                </div>
            </h1>
        }
    }
})